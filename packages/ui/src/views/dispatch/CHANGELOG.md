Dispatch UI: Dispatch Dashboard feature

- Added Dispatch dashboard UI that shows node list, slots usage, and task queue counts.
- Auto-refresh every 10 seconds by querying /api/v1/dispatch endpoints.
- Integrated into sidebar with "Dispatch" menu item.
- Uses existing Material-UI components; no new dependencies.
