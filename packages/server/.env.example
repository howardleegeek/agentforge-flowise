# Oyster Dispatch integration for AgentForge server
# Configure to enable dispatch integration and point to a controller
# Leave values empty or false by default to disable integration during local runs

DISPATCH_ENABLED=false
DISPATCH_CONTROLLER_URL="https://your-dispatch-controller.local"
