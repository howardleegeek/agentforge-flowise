# Oyster Dispatch integration example environment
# Enable/disable the dispatch bridge (true|false)
DISPATCH_ENABLED=false
# Base URL of the Dispatch controller API (no trailing slash)
DISPATCH_CONTROLLER_URL=http://localhost:8000
