# Oyster Dispatch integration environment example
# Enable or disable the dispatch bridge via DISPATCH_ENABLED.
# When enabled, the server will submit Flowise chatflow executions to the dispatch controller.
# Ensure DISPATCH_CONTROLLER_URL points to your Dispatch controller API endpoint.
DISPATCH_ENABLED=false
DISPATCH_CONTROLLER_URL=https://dispatch-controller.local
